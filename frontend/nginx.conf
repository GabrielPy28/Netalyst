# Obsoleto: la imagen Docker usa nginx/templates/default.conf.template
# y la variable NETALYST_API_ORIGIN (sustituida al arrancar el contenedor).
#
# Producción (API en Railway):
#   NETALYST_API_ORIGIN=https://netalyst-production.up.railway.app
# Local (compose):
#   NETALYST_API_ORIGIN=http://api:8000  (valor por defecto en el Dockerfile)
