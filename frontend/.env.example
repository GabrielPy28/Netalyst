# Base URL del API (sin barra final). En desarrollo con proxy de Vite puedes dejar:
# VITE_API_BASE=/api/v1
# Producción (mismo dominio o CORS configurado):
# VITE_API_BASE=https://api.tudominio.com/api/v1
VITE_API_BASE=/api/v1
